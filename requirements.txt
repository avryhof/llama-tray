# llama_tray requirements
# Install with: pip install -r requirements.txt
huggingface_hub
pillow>=10.0.0
requests>=2.28.0   # optional; used by some HF helpers

# pystray is only needed on Windows/macOS, or as a fallback on Linux
# when GTK3/AppIndicator3 is not installed.
# pystray>=0.19.5
